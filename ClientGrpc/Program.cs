using Grpc.Net.Client;
using GrpcService;

internal class Program
{
    private static void Main(string[] args)
    {
 /*       var grpcChannel = GrpcChannel.ForAddress("https://localhost:7082");
        var greeterClient = new Greeter.GreeterClient(grpcChannel);
        var greeterReply = greeterClient.Register(new RegisterRequest
        {
            Username = "quang1",
            Password = "1",
            ConfirmPass = "1"
        });
        Console.WriteLine(greeterReply.ToString());
        Console.ReadKey();*/
    }
}